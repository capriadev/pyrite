# Features - SDD index | delete when complete | new ID = last_id + 1

last_id: 5

<!--
One line per spec. Planned features may be registered here without a spec yet (status: pending); a spec is opened when the feature is planned and ready to develop. When a feature is COMPLETE: delete its line here and the spec file. Never reuse an ID; last_id only grows.

Format: #<id> <name> - <one-line summary> [spec: <file>] [status: pending|active|blocked]
-->

#2 Frontend skeleton - Next.js App Router + TS strict + Tailwind v4 in apps/frontend, gateway api client, status view [status: pending]
#3 Config en DB - settings/keys del sistema en DB cargadas al boot, sin .env [status: pending]
#4 Auth core - login passphrase, Argon2 encadenado, esquema crypto unico [status: pending]
#5 Startup screen - sonido de inicio + mensaje de voz rotativo (ElevenLabs, 2-3 variantes) [status: pending]