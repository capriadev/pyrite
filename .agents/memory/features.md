# Features - SDD index | delete when complete | new ID = last_id + 1

last_id: 0

<!--
One line per spec. Active specs live in specs/. When a feature is COMPLETE: delete its line here and the spec file. Never reuse an ID; last_id only grows.

Format: #<id> <name> - <one-line summary> [spec: <file>] [status: pending|active|blocked]
-->