/**
 * Pyrite frontend - react-doctor config.
 * Local/agent quality tool, not a CI gate (telemetry lives in the CLI flag).
 * Scope: our UI source under src/.
 */
export default {
  include: ["src/**/*.{ts,tsx}"],
};
